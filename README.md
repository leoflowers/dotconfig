dotconfig
=========

Collecting all of my configurations in one place.

notes
-----

To dump conda environment:
``` bash
conda export > environment.yml
```

To install conda environment:
``` bash
conda env create -f /path/to/environment.yml
```
